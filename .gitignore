node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*

# runtime tunnel URL (ephemeral)
.tunnel-url

# server-side rankings DB (runtime user data; server.mjs creates it)
server-data/

# local verification scripts (not shipped)
tg-*.mjs
